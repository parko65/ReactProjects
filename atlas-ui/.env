# Base URL of the local JetMix REST API (resolved via the ces.io hosts entry).
# Mirrors the Blazor client's JetMixApiOptions.BaseUrl. Same value in dev and
# prod because clients reach the API directly on the local network.
VITE_API_BASE_URL=https://ces.io:8085
